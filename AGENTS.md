# 项目级强制规则

## 不可破坏的边界

1. 城市、路线、车辆型号、货物、订单、事件和执照不得写死在界面或规则代码中。
2. 界面不得直接修改现金、车辆状态、订单状态或市场状态；只能发送命令，由规则核心验证并产生事件。
3. 规则核心不得依赖浏览器 DOM、安卓接口、广告接口、文件路径或具体渲染框架。
4. 公路货运、客运、铁路、航运和航空统一实现运输方式协议，不允许复制五套订单或财务逻辑。
5. 所有金额使用整数“分”，里程使用整数“米”，时间使用整数“游戏秒”；显示层负责换算。
6. 已发布的永久编号不得复用或改变含义。内容下架时标记停用，不得把编号给新内容。
7. 存档结构变化必须增加 `saveVersion` 并提供逐版本迁移，不允许要求玩家删档。
8. 新功能默认通过功能开关接入，未完成模块不能污染正式流程。
9. 新增内容优先增加内容包，不修改规则核心。
10. 提交前必须运行内容校验和核心测试。

## 图片工作流隔离

1. 图片生成、查看、缩略图检查、迭代和废稿清理由独立图片子 Agent 完成。
2. 主 Agent 只接收最终资源路径、资源编号、尺寸、格式、版本与一句结果。
3. 禁止 Base64、图片二进制、像素数据、高分辨率预览、中间版本和失败版本进入主上下文。
4. 只有定稿资源可以提升到 `assets/generated/`。
5. 程序只引用资源编号，不引用某次生成任务的临时文件名。
6. 相同生成参数使用内容寻址缓存；任务结束删除废稿，最多保留定稿与一个上版。

## 代码依赖方向

允许：`表现层 -> 应用层 -> 规则核心 <- 数据适配层`

禁止：

- 规则核心导入表现层。
- 数据文件调用界面函数。
- 页面按钮直接执行结算公式。
- 事件处理器绕过命令验证。
- 为单个城市或车型增加专用分支；差异必须由数据或策略实现。

## 修改纪律

- 修改永久数据协议前，先写兼容性说明和迁移方案。
- 删除公共字段前，至少经历一个弃用版本。
- 大功能先写接口、数据格式和验收条件，再写界面。
- UI 可以重做，规则核心与存档身份必须保持稳定。
